node_modules/
.DS_Store
*.log
.env

# Agent skills — installed content + symlinks, regenerated from skills-lock.json (~94 MB, don't vendor)
.agents/
.claude/skills/
.claude/settings.local.json

reference/

# Playwright MCP verification output — screenshots/logs, not part of the theme
.playwright-mcp/
# Any PNG at the repo root is a verification shot; real theme images live in assets/
/*.png
